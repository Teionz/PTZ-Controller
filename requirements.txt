ultralytics>=8.1.0
opencv-python>=4.8.0
requests>=2.28.0
numpy>=1.24.0
PySide6>=6.5.0
pygame>=2.5.0
# matplotlib é dependência REAL do ultralytics (a IA usa mesmo sem gráfico na tela).
# Sozinho o pip já instalaria isso ao instalar o ultralytics; deixamos explícito aqui
# porque foi o que faltou no .exe (estava sendo excluído do empacotamento).
matplotlib>=3.6.0
